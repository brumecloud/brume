type User {
  id: String!
  name: String!
  avatar: String!
  projects: [Project!]!
}

type Project {
  id: String!
  name: String!
  description: String!
}

type Query {
  me: User!
}

type Mutation {
  createProject(name: String!, description: String): Project
}

schema {
  query: Query
  mutation: Mutation
}
