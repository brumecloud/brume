meta {
  name: Tunnel local failure
  type: http
  seq: 14
}

get {
  url: {{failed_tunnel_url}}/inspect
  body: none
  auth: none
}

tests {
  test("an unreachable local service returns bad gateway", function() {
    expect(res.status).to.equal(502);
  });
}
