meta {
  name: Offline tunnel
  type: http
  seq: 13
}

get {
  url: {{offline_url}}/inspect
  body: none
  auth: none
}

tests {
  test("inactive tunnels are hidden", function() {
    expect(res.status).to.equal(404);
  });
}
