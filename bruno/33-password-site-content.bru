meta {
  name: Read password website with scoped session
  type: http
  seq: 33
}

get {
  url: {{password_deployment_url}}/
  body: none
  auth: none
}

tests {
  test("scoped password session loads the hosted website", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.include("Brume static deployment fixture");
  });
}
